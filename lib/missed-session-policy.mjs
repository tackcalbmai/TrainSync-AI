import { getExerciseDefinition } from "./exercise-catalog.mjs";

export const MISSED_SESSION_POLICY_VERSION = "2026-08-28.1";
const ACTIVE_SESSION_STATUSES = new Set(["planned","generated"]);
const LIMITING_FATIGUE_TAGS = new Set(["grip","spinal_bracing","core_bracing","hinge","shoulder_girdle_stability"]);

function arr(value){return Array.isArray(value)?value:[];}
function isoDate(value){const raw=String(value||"").trim();return /^\d{4}-\d{2}-\d{2}$/.test(raw)?raw:null;}
function dateMs(value){const iso=isoDate(value);return iso?Date.parse(`${iso}T12:00:00Z`):NaN;}
function addDays(value,days){const ms=dateMs(value);if(!Number.isFinite(ms))return null;return new Date(ms+Number(days)*86400000).toISOString().slice(0,10);}
function dayGap(a,b){const x=dateMs(a),y=dateMs(b);return Number.isFinite(x)&&Number.isFinite(y)?Math.round(Math.abs(x-y)/86400000):Infinity;}
function unique(values){return [...new Set(values.filter(Boolean))];}
function exerciseMeta(exercise){const canonical=getExerciseDefinition(exercise?.exerciseKey||exercise?.name);return canonical||exercise||{};}
function sessionFootprint(session){const exercises=arr(session?.payload?.exercises);const primary=new Set(),fatigue=new Set();let strengthPriority=false;for(const exercise of exercises){const meta=exerciseMeta(exercise);for(const muscle of arr(meta.primaryMuscles??meta.primary_muscles))primary.add(muscle);for(const tag of arr(meta.fatigueTags??meta.fatigue_tags))if(LIMITING_FATIGUE_TAGS.has(tag))fatigue.add(tag);if(String(exercise?.role||"").toLowerCase().includes("strength"))strengthPriority=true;}return{primary:[...primary],fatigue:[...fatigue],strengthPriority};}
function overlap(left,right){const a=new Set(left),b=new Set(right);if(!a.size||!b.size)return{shared:[],ratio:0};const shared=[...a].filter((x)=>b.has(x));return{shared,ratio:shared.length/Math.min(a.size,b.size)};}

export function findMissedProgramSessions(sessions=[],todayIso){const today=isoDate(todayIso);if(!today)throw new Error("TODAY_ISO_REQUIRED");return arr(sessions).filter((session)=>ACTIVE_SESSION_STATUSES.has(String(session?.status||""))&&isoDate(session?.scheduled_date)&&session.scheduled_date<today).sort((a,b)=>String(a.scheduled_date).localeCompare(String(b.scheduled_date))||Number(a.day_index||0)-Number(b.day_index||0));}

export function assessMissedSessionMove({missedSession,candidateDate,sessions=[]}={}){const date=isoDate(candidateDate);if(!missedSession?.id||!date)return{allowed:false,reasonCode:"MOVE_CONTEXT_REQUIRED",warnings:[]};const others=arr(sessions).filter((session)=>session?.id!==missedSession.id&&ACTIVE_SESSION_STATUSES.has(String(session?.status||""))&&isoDate(session?.scheduled_date));if(others.some((session)=>session.scheduled_date===date))return{allowed:false,reasonCode:"SESSION_ALREADY_SCHEDULED_ON_DATE",warnings:[]};const missedFootprint=sessionFootprint(missedSession);const warnings=[];for(const other of others){const gap=dayGap(date,other.scheduled_date);if(gap!==1)continue;const otherFootprint=sessionFootprint(other);const primary=overlap(missedFootprint.primary,otherFootprint.primary);const fatigue=overlap(missedFootprint.fatigue,otherFootprint.fatigue);if(primary.ratio>=0.5&&primary.shared.length){return{allowed:false,reasonCode:"ADJACENT_HIGH_PRIMARY_OVERLAP",conflictSessionId:other.id,conflictDate:other.scheduled_date,sharedPrimary:primary.shared,warnings};}if(fatigue.shared.length&&(missedFootprint.strengthPriority||otherFootprint.strengthPriority))warnings.push({code:"ADJACENT_SHARED_FATIGUE",sessionId:other.id,date:other.scheduled_date,tags:fatigue.shared});else warnings.push({code:"ADJACENT_SESSION",sessionId:other.id,date:other.scheduled_date});}
return{allowed:true,reasonCode:warnings.length?"MOVE_ALLOWED_WITH_ADJACENCY_CAUTION":"MOVE_ALLOWED",warnings,policyVersion:MISSED_SESSION_POLICY_VERSION};}

export function missedSessionOptions({missedSession,sessions=[],todayIso,maxSearchDays=3}={}){const today=isoDate(todayIso);if(!today||!missedSession?.id)throw new Error("MISSED_SESSION_CONTEXT_REQUIRED");if(!ACTIVE_SESSION_STATUSES.has(String(missedSession.status||""))||!isoDate(missedSession.scheduled_date)||missedSession.scheduled_date>=today)return{missed:false,moveOptions:[],skipAllowed:false,reasonCode:"SESSION_NOT_MISSED",policyVersion:MISSED_SESSION_POLICY_VERSION};const future=arr(sessions).filter((session)=>session?.id!==missedSession.id&&ACTIVE_SESSION_STATUSES.has(String(session?.status||""))&&isoDate(session.scheduled_date)&&session.scheduled_date>=today).sort((a,b)=>String(a.scheduled_date).localeCompare(String(b.scheduled_date)));const nextFuture=future[0]?.scheduled_date||null;const dates=[];for(let offset=0;offset<=Math.max(0,Math.min(7,Number(maxSearchDays)||3));offset+=1){const candidate=addDays(today,offset);if(nextFuture&&candidate>=nextFuture)break;dates.push(candidate);}const moveOptions=dates.map((candidateDate)=>({candidateDate,...assessMissedSessionMove({missedSession,candidateDate,sessions})})).filter((item)=>item.allowed);return{missed:true,missedSessionId:missedSession.id,originalDate:missedSession.scheduled_date,nextScheduledDate:nextFuture,moveOptions,skipAllowed:true,catchUpVolume:false,reasonCode:moveOptions.length?"MISSED_SESSION_OPTIONS_AVAILABLE":"NO_SAFE_AUTOMATIC_MOVE_BEFORE_NEXT_SESSION",policyVersion:MISSED_SESSION_POLICY_VERSION};}

export function missedSessionResolutionAudit({missedSession,action,movedTo=null,assessment=null}={}){if(!missedSession?.id)throw new Error("MISSED_SESSION_REQUIRED");if(action==="skip")return{adjustment_type:"schedule",reason_code:"MISSED_SESSION_SKIPPED",reason_text:"A missed session was skipped and the remaining program continues without automatically adding catch-up volume.",evidence_level:"heuristic",science_version:`schedule:${MISSED_SESSION_POLICY_VERSION}`,evidence_claim_ids:[],evidence_rule_keys:[],decision_confidence:1,decision_source:"user_confirmed",before_state:{scheduledDate:missedSession.scheduled_date,status:missedSession.status},after_state:{scheduledDate:missedSession.scheduled_date,status:"skipped"},metrics_snapshot:{catchUpVolume:false}};if(action==="move"&&isoDate(movedTo)&&assessment?.allowed)return{adjustment_type:"schedule",reason_code:"MISSED_SESSION_MOVED",reason_text:"The missed session was moved to a user-confirmed date after deterministic spacing checks; no other session or weekly volume was automatically changed.",evidence_level:"heuristic",science_version:`schedule:${MISSED_SESSION_POLICY_VERSION}`,evidence_claim_ids:[],evidence_rule_keys:[],decision_confidence:1,decision_source:"user_confirmed",before_state:{scheduledDate:missedSession.scheduled_date,status:missedSession.status},after_state:{scheduledDate:movedTo,status:missedSession.status},metrics_snapshot:{catchUpVolume:false,warnings:arr(assessment.warnings)}};throw new Error("MISSED_SESSION_RESOLUTION_INVALID");}
