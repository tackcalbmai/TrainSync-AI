APP_URL=http://localhost:3000
TRAINING_PROVIDER=mock
DEFAULT_TIMEZONE=Europe/Riga

# Supabase public project config. The current no-build PWA uses the publishable key in
# lib/supabase-client.js. Publishable keys are safe for browser use; RLS is mandatory.
SUPABASE_URL=https://sjihbrpbhfttuyzmbfku.supabase.co
SUPABASE_PUBLISHABLE_KEY=

# Garmin Developer Program (after approval)
GARMIN_CLIENT_ID=
GARMIN_CLIENT_SECRET=
GARMIN_REDIRECT_URI=
TOKEN_ENCRYPTION_KEY=

# Optional: web-app AI generation. ChatGPT MCP flow does not require this yet.
OPENAI_API_KEY=
