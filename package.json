{
  "name": "trainsync-ai",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Strength-first AI training control layer for Garmin Connect.",
  "scripts": {
    "dev": "node server.mjs",
    "test": "node --test",
    "check": "node --check server.mjs && node --check app.js && node --check lib/workout.mjs && node --check lib/mock-garmin.mjs && node --check lib/supabase-client.js && node --check api/generate.js && node --check api/publish.js && node --check api/validate.js"
  },
  "engines": {
    "node": "22.x"
  }
}
